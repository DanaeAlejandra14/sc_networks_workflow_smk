This repository ...

## Estructura de datos
- `data/`: matrices de conteo crudas por muestra (input del pipeline, referenciadas en `config/samples.tsv`).
- `resources/`: recursos de referencia y herramientas auxiliares (no específicos de una muestra).
